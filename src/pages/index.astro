---
import Layout from '@/layouts/Layout.astro'
import Hero from '@/components/Hero.astro'
import FeaturedProjects from '@/components/FeaturedProjects.astro'
---

<Layout>
  <main>
    <Hero />
    <section class="about">
      <div class="about-content">
        <div>
          <h2>About Me</h2>
          <p>
            I'm a developer based in Las Vegas who loves crafting web
            experiences that feel effortless to navigate and respond instantly
            to user interactions. My front-end focus gives me a strong eye for
            detail, while my full-stack experience helps me build reliable
            solutions end to end.
          </p>
        </div>
        <ul class="values">
          <li>
            <strong>Fast-feeling interfaces</strong>
            <span>Responsive layouts and interactions that stay out of the user's way.</span>
          </li>
          <li>
            <strong>Maintainable component systems</strong>
            <span>Clear structure that makes products easier to extend.</span>
          </li>
          <li>
            <strong>Details that reduce friction</strong>
            <span>Small decisions that make workflows feel simpler.</span>
          </li>
        </ul>
      </div>
    </section>
    <FeaturedProjects heading="Featured Projects" />
  </main>
</Layout>

<style>
  main {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    min-height: 92vh;

    .about {
      margin: 0 1rem 4rem;
      width: 100%;

      .about-content {
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(280px, 0.8fr);
        align-items: center;
        gap: 2.5rem;

        h2 {
          font-size: 2rem;
          margin-bottom: 1rem;
        }

        p {
          color: var(--text-muted);
          line-height: 1.7;
          font-size: 1.1rem;
          max-width: 65ch;
          margin: 0;
        }

        .values {
          display: flex;
          flex-direction: column;
          gap: 1rem;
          padding: 0;
          margin: 0;
          list-style: none;

          li {
            flex-direction: column;
            display: flex;
            gap: 0.25rem;
            padding: 1rem 0;
            border-bottom: 1px solid var(--border-color);

            &:last-child {
              border-bottom: none;
            }

            strong {
              color: var(--text-primary);
            }

            span {
              color: var(--text-muted);
            }
          }
        }
      }

      @media (max-width: 768px) {
        .about-content {
          grid-template-columns: 1fr;
          text-align: center;

          p {
            margin: 0 auto;
          }
        }
      }
    }
  }
</style>
